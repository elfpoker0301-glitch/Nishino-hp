backend:
  name: git-gateway
  branch: main

# 日本語対応
locale: ja

# メディアファイルの保存場所
media_folder: "images/uploads"
public_folder: "/images/uploads"

# コレクション設定
collections:
  # お知らせ管理
  - name: "news"
    label: "お知らせ"
    label_singular: "お知らせ記事"
    folder: "content/news"
    create: true
    slug: "{{year}}-{{month}}-{{day}}-{{slug}}"
    fields:
      - {label: "タイトル", name: "title", widget: "string", required: true}
      - {label: "公開日", name: "date", widget: "datetime", format: "YYYY-MM-DD", date_format: "YYYY-MM-DD", time_format: false, required: true}
      - label: "カテゴリ"
        name: "category"
        widget: "select"
        required: true
        options:
          - {label: "お知らせ", value: "info"}
          - {label: "施工情報", value: "work"}
          - {label: "重要", value: "important"}
      - {label: "本文", name: "body", widget: "markdown", required: true}
      - {label: "公開", name: "published", widget: "boolean", default: true}
    
    # 一覧表示の設定
    summary: "{{title}} - {{date}}"
    sortable_fields: ['date', 'title', 'category']
    view_filters:
      - label: "お知らせ"
        field: category
        pattern: "info"
      - label: "施工情報"
        field: category
        pattern: "work"
      - label: "重要"
        field: category
        pattern: "important"
      - label: "公開中"
        field: published
        pattern: true
      - label: "下書き"
        field: published
        pattern: false

  # 工事実績管理
  - name: "works"
    label: "工事実績"
    label_singular: "工事実績"
    folder: "content/works"
    create: true
    slug: "{{year}}-{{month}}-{{day}}-{{slug}}"
    fields:
      - {label: "タイトル", name: "title", widget: "string", required: true}
      - {label: "施工日", name: "date", widget: "datetime", format: "YYYY-MM-DD", date_format: "YYYY-MM-DD", time_format: false, required: true}
      - {label: "カテゴリ", name: "category", widget: "select", options: ["ダイヤモンドコア工事", "アンカー工事", "非破壊検査", "その他"], required: true}
      - {label: "場所", name: "location", widget: "string", required: true}
      - {label: "説明", name: "description", widget: "text", required: true}
      - {label: "画像", name: "image", widget: "image", required: true, media_folder: "/images/works", public_folder: "/images/works"}
      - {label: "詳細", name: "body", widget: "markdown", required: false}
      - {label: "公開", name: "published", widget: "boolean", default: true}
    
    # 一覧表示の設定
    summary: "{{title}} - {{location}} ({{date}})"
    sortable_fields: ['date', 'title', 'category', 'location']
    view_filters:
      - label: "ダイヤモンドコア工事"
        field: category
        pattern: "ダイヤモンドコア工事"
      - label: "アンカー工事"
        field: category
        pattern: "アンカー工事"
      - label: "非破壊検査"
        field: category
        pattern: "非破壊検査"
      - label: "公開中"
        field: published
        pattern: true
      - label: "下書き"
        field: published
        pattern: false

  # 事業案内管理
  - name: "services"
    label: "事業案内"
    label_singular: "事業"
    folder: "content/services"
    create: true
    slug: "{{slug}}"
    fields:
      - {label: "事業名", name: "title", widget: "string", required: true}
      - {label: "サブタイトル（英語）", name: "subtitle", widget: "string", required: false}
      - {label: "アイコン（絵文字）", name: "icon", widget: "string", default: "🏗️", required: true}
      - {label: "簡単な説明", name: "description", widget: "text", required: true}
      - {label: "詳細説明", name: "body", widget: "markdown", required: true}
      - {label: "表示順", name: "order", widget: "number", default: 1, value_type: "int", min: 1}
      - {label: "公開", name: "published", widget: "boolean", default: true}
    
    # 一覧表示の設定
    summary: "{{icon}} {{title}}"
    sortable_fields: ['order', 'title']
